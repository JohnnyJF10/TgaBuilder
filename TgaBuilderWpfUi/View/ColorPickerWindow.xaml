<elements:AsyncWindow
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:elements="clr-namespace:TgaBuilderWpfUi.Elements"
    xmlns:local="clr-namespace:PropertyTools.Wpf"
    mc:Ignorable="d"
    x:Class="TgaBuilderWpfUi.View.ColorPickerWindow"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterOwner"
    SizeToContent="WidthAndHeight"
    ResizeMode="NoResize"
    Title="Color Picker"
    d:Background="Black">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/TitleBarNavigationAreaStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <DockPanel Grid.Row="0">
            <ui:TitleBar
                DockPanel.Dock="Right"
                Width="133"
                Style="{StaticResource TitleBarNavigationAreaStyle}"/>
            <ui:TitleBar
                Title="Color Picker"
                ShowMinimize="False"
                ShowMaximize="False"
                ShowClose="False"
                Margin="3,3,0,0">
                <ui:TitleBar.Icon>
                    <ui:ImageIcon Source="pack://application:,,,/Resources/TgaBuilder_picture.png" />
                </ui:TitleBar.Icon>
            </ui:TitleBar>
        </DockPanel>

        <!-- Content -->
        <Border Grid.Row="1" Padding="16">
            <StackPanel>

                <!-- Color Pickers Side-by-Side -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Source Color -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Source Color"
                                   HorizontalAlignment="Center"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6"/>
                        <local:ColorPickerPanel x:Name="SourceColorPanel" />
                    </StackPanel>

                    <!-- Target Color -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Target Color"
                                   HorizontalAlignment="Center"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6"/>
                        <local:ColorPickerPanel x:Name="TargetColorPanel" />
                    </StackPanel>
                </Grid>

                <!-- Separator -->
                <Separator Margin="0,12"/>

                <!-- Buttons -->
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button x:Name="CancelButton"
                            Content="Cancel"
                            MinWidth="84"
                            Margin="0,0,10,0"
                            HorizontalContentAlignment="Center" />
                    <Button x:Name="OkButton"
                            Content="OK"
                            MinWidth="84"
                            HorizontalContentAlignment="Center" />
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</elements:AsyncWindow>
